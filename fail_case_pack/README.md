# Fail case pack (Week 11 — Deliverable 4)

Add **one** failure example for the team.

## Files (suggested)

| File | Purpose |
|------|---------|
| `screenshot.png` (or `.jpg`) | Bad output as shown in UI / browser / JSON viewer |
| `failure_notes.md` | Short text template below |

**Filled for Week 11:** [`failure_notes.md`](./failure_notes.md) and [`screenshot.png`](./screenshot.png) (Module 1 — missing API key → heuristic-only labels).

## `failure_notes.md` template

```markdown
# Fail case — [Module X] — [date]

## What failed (one sentence)
[ e.g. Module 3 card invented a product name not present in the trend evidence. ]

## Fix we will try next week (one sentence)
[ e.g. Add a post-processing guard that rejects cards when proper nouns don’t appear in Module 1 snippets. ]
```

Replace the bracketed lines with real content. Keep screenshots **free of PII** unless reviewers approved.
